<% if (!user) { %>
<div class="hero-unit">
  <h1>欢迎来到 Microblog</h1>
  <p>Microblog 是一个基于Node.JS的微博系统。</p>
  <p>
    <a class="btn btn-primary btn-large" href="/login">登录</a>
    <a class="btn btn-large" href="/regist">立即注册</a>
  </p>
</div>
<% } else { %>
  <%- partial('./partials/say') %>
<% } %>
  <%- partial('./partials/posts') %>
